name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v7
      - uses: extractions/setup-just@v4
      - run: just ci
